import React from "react";
import quickMenuStyle from "./quickMenu.module.scss";

export default function Navigation() {
  return (
    <nav className={quickMenuStyle.wrapper}>
      <p>quick menu</p>
    </nav>
  );
}
