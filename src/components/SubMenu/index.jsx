import React from "react";
import subMenuStyle from "./subMenu.module.scss";

export default function SubMenu() {
  return (
    <nav className={subMenuStyle.wrapper}>
      <p>quick menu</p>
    </nav>
  );
}
